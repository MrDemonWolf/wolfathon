import { OverlaysTab } from "@/components/control/overlays-tab";

/** Settings section: tokenized OBS overlay URLs + reset. */
export default function SettingsOverlaysPage() {
	return <OverlaysTab />;
}
