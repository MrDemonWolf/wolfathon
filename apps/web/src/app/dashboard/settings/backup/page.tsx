import { BackupTab } from "@/components/control/backup-tab";

/** Settings section: import/export for rewards + timer state, one home. */
export default function SettingsBackupPage() {
	return <BackupTab />;
}
