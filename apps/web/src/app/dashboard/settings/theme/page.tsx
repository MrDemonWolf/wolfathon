import { ThemeTab } from "@/components/control/theme-tab";

/** Settings section: the global overlay theme, shared by the timer + rewards overlays. */
export default function SettingsThemePage() {
	return <ThemeTab />;
}
