import { OverlaysTab } from "@/components/control/overlays-tab";

/** Control section: tokenized OBS overlay URLs + reset. */
export default function ControlOverlaysPage() {
	return <OverlaysTab />;
}
