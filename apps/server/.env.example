# Origin allowed to call the overlay API (your web app origin).
# Dev: the Next app runs on :3001.
# Production: https://wolfathon.mrdemonwolf.workers.dev
# (If unset at deploy, infra defaults CORS_ORIGIN to the production web URL.)
CORS_ORIGIN=http://localhost:3001
