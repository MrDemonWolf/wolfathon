{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@tmp-wolfathon/api": "workspace:*",
    "@tmp-wolfathon/db": "workspace:*",
    "@tmp-wolfathon/env": "workspace:*",
    "@trpc/server": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@tmp-wolfathon/config": "workspace:*",
    "@types/node": "^22.13.14",
    "alchemy": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
